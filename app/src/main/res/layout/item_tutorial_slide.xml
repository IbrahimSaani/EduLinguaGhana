<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp">

    <!-- Big Playful Emoji -->
    <TextView
        android:id="@+id/tvSlideEmoji"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🌍"
        android:textSize="130sp"
        app:layout_constraintBottom_toTopOf="@+id/guideline"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.4" />

    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/guideline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        app:layout_constraintGuide_percent="0.45" />

    <!-- Mascot Presenter -->
    <com.airbnb.lottie.LottieAnimationView
        android:id="@+id/ivMascotTutorial"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_marginStart="8dp"
        app:lottie_rawRes="@raw/angry_owl"
        app:lottie_autoPlay="true"
        app:lottie_loop="true"
        android:contentDescription="Mascot"
        app:layout_constraintBottom_toTopOf="@+id/bubbleCard"
        app:layout_constraintStart_toStartOf="parent"
        android:translationY="30dp"
        android:elevation="6dp"/>

    <!-- Speech Bubble Card -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/bubbleCard"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardCornerRadius="32dp"
        app:cardElevation="8dp"
        app:strokeWidth="4dp"
        app:strokeColor="@color/white"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/guideline"
        app:layout_constraintVertical_bias="0.2">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="28dp"
            android:background="@color/cardBackground">

            <TextView
                android:id="@+id/tvSlideTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="@font/agbalumo"
                android:text="Welcome"
                android:textColor="@color/colorPrimary"
                android:textSize="32sp"
                android:textStyle="bold"
                android:layout_marginBottom="12dp"/>

            <TextView
                android:id="@+id/tvSlideDescription"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:lineSpacingExtra="8dp"
                android:text="Learn Ghanaian languages with ease and fun."
                android:textColor="@color/textColorPrimary"
                android:fontFamily="sans-serif-medium"
                android:textSize="18sp" />

        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- Decorative Sparkles -->
    <TextView
        android:id="@+id/tvSparkle1"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="✨"
        android:textSize="24sp"
        app:layout_constraintEnd_toEndOf="@+id/tvSlideEmoji"
        app:layout_constraintTop_toTopOf="@+id/tvSlideEmoji" />

    <TextView
        android:id="@+id/tvSparkle2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🌟"
        android:textSize="20sp"
        app:layout_constraintBottom_toBottomOf="@+id/tvSlideEmoji"
        app:layout_constraintStart_toStartOf="@+id/tvSlideEmoji" />

</androidx.constraintlayout.widget.ConstraintLayout>
